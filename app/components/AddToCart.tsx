"use client";

const AddToCart = () => {
  return (
    <div className="mt-5">
      <button
        className="btn btn-sm text-xs"
        onClick={() => console.log("Clicked")}
      >
        Add to cart
      </button>
    </div>
  );
};

export default AddToCart;
