"use client";

const AddToCart = () => {
  return (
    <button
      onClick={() => console.log("Lorem")}
      className="bg-green-500 rounded px-5 text-white py-1 text-sm font-mono shadow"
    >
      Add to Cart
    </button>
  );
};

export default AddToCart;
